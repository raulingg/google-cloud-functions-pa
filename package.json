{
  "name": "full-text-search",
  "description": "Full text search for Firestore",
  "scripts": {
    "serve": "cd public && live-server --host=localhost --port=8888 --entry-file=index.html",
    "serve:watch": "npm run serve -- --watch"
  },
  "devDependencies": {
    "live-server": "^1.2.1"
  },
  "private": true
}
